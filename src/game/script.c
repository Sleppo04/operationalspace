#include "script.h"

int Script_ComputeShipActions(script_t* script, game_t* game)
{
    if (script == NULL) {
        return EINVAL;
    }
    if (game == NULL) {
        return EINVAL;
    }

    return 0;
}